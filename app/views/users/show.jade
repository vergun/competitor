extends ../layouts/default

block main
block content
  h1=user.name
  hr
  h2=user.email
  h2=user.username
  h2=user.keywords
  p.offset2.message=message
  a(href="/users/"+req.user.id + "/edit", title="Edit Profile") Edit Profile